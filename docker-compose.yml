# ============================================
# SONORA VOICE - Servidor WebRTC
# Despliegue manual (sin Coolify/Traefik)
# ============================================
services:
  voice:
    build:
      context: .
      dockerfile: Dockerfile
    # Puerto expuesto DIRECTAMENTE al host
    ports:
      - "7860:7860"
    env_file:
      - .env
    restart: unless-stopped
